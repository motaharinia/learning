package com.motaharinia.javacore.rjavacollection.gjavalinkedhashset;

/**
 * https://www.javatpoint.com/java-linkedhashset
 * Java LinkedHashSet class is a Hashtable and Linked list implementation of the set interface.
 * It inherits HashSet class and implements Set interface.
 * The important points about Java LinkedHashSet class are:
 * Java LinkedHashSet class contains unique elements only like HashSet.
 * Java LinkedHashSet class provides all optional set operation and permits null elements.
 * Java LinkedHashSet class is non synchronized.
 * Java LinkedHashSet class maintains insertion order.
 * کلاس Java LinkedHashSet اجرای لیست Hashtable و Linked از رابط مجموعه است.
 * این کلاس HashSet را به ارث می برد و رابط Set را پیاده سازی می کند.
 * نکات مهم در مورد کلاس Java LinkedHashSet عبارتند از:
 * کلاس Java LinkedHashSet شامل عناصر منحصر به فرد فقط مانند HashSet است.
 * کلاس Java LinkedHashSet تمام عملکرد ست اختیاری را فراهم می کند و عناصر تهی را مجاز می سازد.
 * کلاس Java LinkedHashSet هماهنگ نیست.
 * کلاس Java LinkedHashSet ترتیب درج را حفظ می کند.
 *
 * Hierarchy of LinkedHashSet class:
 * The LinkedHashSet class extends HashSet class which implements Set interface.
 * The Set interface inherits Collection and Iterable interfaces in hierarchical order.
 *سلسله مراتب از کلاس LinkedHashSet:
 * کلاس LinkedHashSet کلاس HashSet را گسترش می دهد که رابط Set را پیاده سازی می کند.
 * رابط Set به ترتیب سلسله مراتبی از رابط های مجموعه و Iterable به ارث می برد.
 *
 * LinkedHashSet class declaration:
 * Let's see the declaration for java.util.LinkedHashSet class.
 * public class LinkedHashSet<E> extends HashSet<E> implements Set<E>, Cloneable, Serializable
 *
 * Constructors of Java LinkedHashSet class:
 * HashSet():
 * It is used to construct a default HashSet.
 * HashSet(Collection c):
 * It is used to initialize the hash set by using the elements of the collection c.
 * LinkedHashSet(int capacity):
 * It is used initialize the capacity of the linked hash set to the given integer value capacity.
 * LinkedHashSet(int capacity, float fillRatio):
 * It is used to initialize both the capacity and the fill ratio (also called load capacity) of the hash set from its argument.
 * سازندگان کلاس Java LinkedHashSet:
 * HashSet ():
 * برای ساختن پیش فرض HashSet استفاده می شود.
 * HashSet(Collection c):
 * برای اولیه سازی هش تنظیم شده با استفاده از عناصر مجموعه c استفاده می شود.
 * LinkedHashSet(int capacity):
 * استفاده می شود مقدار اولیه هش مرتبط با ظرفیت ارزش عدد صحیح داده شده را مقدار دهی می کند.
 * LinkedHashSet(int capacity, float fillRatio):
 * این برای بدست آوردن هم ظرفیت و هم نسبت پر (که به آن ظرفیت بار نیز گفته می شود) از هش مجموعه شده از استدلال آن استفاده می شود
 *
 *
 */
public class JavaLinkedHashSet {
}
