package com.motaharinia.javacore.gjavaoopsmisc.koverloadingvsoverriding;

/**
 * Difference between method overloading and method overriding in java:
 * 1)	Method overloading is used to increase the readability of the program.
 * Method overriding is used to provide the specific implementation of the method that is already provided by its super class.
 * 2)	Method overloading is performed within class.
 * Method overriding occurs in two classes that have IS-A (inheritance) relationship.
 * 3)	In case of method overloading, parameter must be different.
 * In case of method overriding, parameter must be same.
 * 4)	Method overloading is the example of compile time polymorphism.
 * Method overriding is the example of run time polymorphism.
 * 5)	In java, method overloading can't be performed by changing return type of the method only.
 * Return type can be same or different in method overloading. But you must have to change the parameter.
 * Return type must be same or covariant in method overriding.
 * تفاوت بین overloading متد و overriding متد در جاوا:
 * 1)از overloading برای افزایش خوانایی برنامه استفاده می شود.
 * از overriding متد برای اجرای خاص متدی که قبلاً توسط سوپر کلاس آن ارائه شده است مورد استفاده قرار می گیرد.
 * 2)حالت overloading متد در کلاس انجام می شود.
 *حالت overriding در دو کلاس رخ می دهد که رابطه IS-A (ارث) دارند.
 * 3) در صورت overloading متد ، پارامتر باید متفاوت باشد.
 * در صورت overriding متد ، پارامتر باید یکسان باشد.
 * 4)حالت overloading متد نمونه  پلی مورفیسم در زمان کامپایل است.
 * حالت overriding نمونه ای از پلی مورفیسم در زمان اجرا است.
 * 5) در جاوا ،overloading متد فقط با تغییر نوع بازگشت متد انجام نمی شود.
 * نوع خروجی می تواند در overloading یکسان یا متفاوت باشد. اما باید پارامتر را تغییر دهید.
 * نوع خروجی می تواند در overriding متد یکسان یا کووارینت باشد.
 */
public class OverloadOverride {
}
